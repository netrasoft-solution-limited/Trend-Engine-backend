from django.apps import AppConfig


class EvidenceConfig(AppConfig):
    """L2 · Normalized evidence · GLOBAL."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.evidence"
    label = "evidence"
    verbose_name = "L2 · Normalized evidence"
