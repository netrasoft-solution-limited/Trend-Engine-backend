from django.apps import AppConfig


class DomainsConfig(AppConfig):
    """L3 · Domain packs · GLOBAL."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.domains"
    label = "domains"
    verbose_name = "L3 · Domain packs"
