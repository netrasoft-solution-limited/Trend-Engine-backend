from django.apps import AppConfig


class OperationsConfig(AppConfig):
    """cross · Health, cost, audit · GLOBAL."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.operations"
    label = "operations"
    verbose_name = "cross · Health, cost, audit"
